# Changelog

本 plugin 所有版本变更记录。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/),版本号遵循 [SemVer 2.0.0](https://semver.org/lang/zh-CN/)。

---

## [1.13.0] - 2026-06-08

### 新增(prd2prototype · 按钮/交互事件清单铁律)

- 第 6 步新增铁律「按钮 / 交互事件清单」:每页 proto-note 放一张清单表,新增页全量列、修改页只列本迭代改动部分,含弹窗/抽屉内部。固定列:位置 / 按钮·交互 / 触发后行为 / 校验·状态变化。
- **范围只收两类**(踩坑:范围别贪大):① 按钮级操作(按钮、行内操作链接、可点击表格列);② 有联动交互的输入控件(改了它会引起其他字段/行/区块变化)。普通录入/筛选控件不列——字段规格表已覆盖,再列就是重复。
- **位置列必须写全层级路径**(Tab → 区块 → 弹窗/抽屉):同名按钮(查询/重置/新增/导入…)到处出现,只写按钮名分不清是哪一个。
- **只列"可操作"的交互**:清单目的是告诉研发哪里可以操作;需求标签 i 图标等原型辅助元素不列,"整表只读无交互"占位行不列;改动部分无任何可操作交互的页面整张清单不放。
- 检查清单(B 交互完整性)同步加 2 个勾选项。

### 优化(prd2prototype · 原型说明口吻与版本记录)

- 大前提新增「不写实现侧自述」踩坑:原型说明/index 不出现"基于 XX 设计系统/common.css/主色 #007D7B"这类实现细节——颜色基线是 skill 内部约定,不是需求;评审与研发只需要业务规则与功能口径。
- index.html 铁律细化:「版本变更记录」必须独立成章(与「PRD 摘要」同级同样式的 section 标题),不要埋在"原型设计说明"卡片内部的表格里——埋在卡片里评审找不到。
- 检查清单(C 文案合规)同步加 2 个勾选项。

## [1.12.0] - 2026-06-02

### 新增(requirements2prd · 发散工具箱)

- 新增「发散工具箱(按需取用 · 发散补强)」一节,补本 skill"只会收敛、不会发散"的短板,方法源自 `product-management:product-brainstorming`,裁剪后只留 4 个真正补强的:
  - **反向法**(挂第 2 步探索约束):反问"怎么把这事做砸",每条反过来就是一条校验规则/约束。
  - **强制多方案**(挂设计岔路口):≥3 方案 + 权衡表再 `AskUserQuestion`,防"第一个还行就拍板"(对应错 6)。
  - **假设测试**(挂探索约束后):列假设 → 标最危险 → 最便宜验证(常是"调一页真实数据")。
  - **发散口**(需求规模定级后、进七步法前):仅路径 A,用 HMW + 反向法先发散,防过早把需求框死。
- 全节贯彻"不堆框架"纪律:每个工具标注"何时用 / 何时别用",并附一句话总表;一次对话用 0~2 个即可。

### 优化(requirements2prd · 触发口径)

- description 补第 ④ 条触发场景与口语关键词(聊需求 / 聊个需求 / 聊个新需求 / 新需求 / 看个需求 / 捋需求),解决"用户口语开场 + 给了需求材料却没触发本 skill"的漏判。

---

## [1.12.0] - 2026-06-03

### 新增(prd2prototype · 大前提)

- **SKILL.md 顶部新增「大前提(铁律):站在产品经理视角,用自然语言输出需求说明」**,置于"心法"之前,作为做原型的根本立场:
  - 原型是产品经理表达需求的载体,不是设计稿、不是代码;说明要让评审/设计/开发/业务都读懂"想要什么",而非技术实现。
  - 一切说明用自然语言、业务口吻,讲清"谁在这里做什么、为什么、点了会怎样";先把需求用人话讲清再落界面;英文技术术语一律翻成中文业务词。
  - 该大前提贯穿全流程,统领第 6 步"原型说明"与第 7 步"文案来源"。

---

## [1.11.0] - 2026-06-01

### 新增(prd2prototype · 内置通用设计系统基线)

- **从 Figma组件库抽取并固化了一套设计系统基线**,作为 prd2prototype 的 `assets/`,做原型直接拷来用,不再从零造 common.css:
  - `assets/common.css`:`:root` 全量设计 token(主色绿 `#007D7B` 主色、功能色、文字/背景/描边、圆角、间距、字体、阴影,含深浅主题)+ 30+ 组件,状态走 CSS 伪类。覆盖按钮(7 类型×5 状态×图标)、输入/选择/多选单选开关/日期、表格/标签/徽标/分页/步骤条/树、标签页/分段/菜单/面包屑/锚点、Alert/Message/Notification/弹窗/缺省页、分级标签等。
  - `assets/common.js`:交互层,Select / TreeSelect / Tree / Modal / 标签页 / 分段 / 多选单选开关按类名 + `data-*` 自动绑定,原型"能点"。
  - `assets/skill-extras.css`:本 skill 的原型辅助层(`.snippet` / `.summary-card` / `.field-spec` / `.proto-note` 等,非 Figma 组件)。
  - `assets/组件规格表.md`(62 组件集真实取色)、`assets/组件样例.html`(全组件演示)、`assets/组件清单.md`(覆盖核对)。
- **改写 SKILL.md**:第 2 步「定义设计语言」改为直接用内置基线;第 3 步组件表更新为基线真实类名;原型规范配色表替换为通用真实色值(`#007D7B` 等);检查清单 A 项对齐到「引 assets、用 token/类名、不硬编码」。

### 来源
- 通用 Figma「组件库-copy0520.fig」解析(kiwi + zstd 解码),抽取 token 与 62 组件集变体配色;统一新版主色 `#007D7B`(输入聚焦 `#0FA081`),旧版 `#00706B` 套件与图标(交设计)不纳入。

---

## [1.10.1] - 2026-05-25

### 修复(requirements2prd · 触发口径)

- **补全"既有模块优化/重构"的触发场景**:原描述虽写了"适用历史模块的优化重构",但触发场景与关键词全部围绕"粗需求→PRD / 新模块",导致用户**只说"讨论某模块怎么优化/重构、怎么从项目拼凑变产品"而没说"做 PRD"**时不触发。
- description 新增触发场景 ③(讨论/推进既有模块优化、重构、通用性/标准化提升、产品化、抽象产品本质、还历史债务,"即使只说讨论/梳理也应触发"),并补关键词:优化、重构、产品化、通用性、标准化、缝合怪、项目拼凑、模块优化、历史债务、产品本质、抽象、第一性原理。

### 来源
- 通用「IP 地址管理」重构讨论:用户开场"讨论…需要优化/重构…使得更像一个产品,而非项目模式拼凑出来的功能",未自动触发本 skill,事后由用户点出补救。

---

## [1.10.0] - 2026-05-25

### 新增(prd2zentao · 真实跑通后的实战沉淀)

- **执行模式拆成 A / B 两种**,按禅道环境选:
  - **模式 A**:Claude in Chrome 自动 navigate + `javascript_tool` 直接跑(公网 / 策略放行的禅道)。
  - **模式 B**:Claude 生成**自包含的批量 upsert 控制台脚本**,用户在自己已登录的禅道标签 `F12 → Console` 粘贴执行、回传 `console.table` 结果给 Claude 核对(内网禅道实战常用)。
- **内嵌自包含批量脚本模板**:替代原先指向外部 `zentao-client.js` 的路径依赖。模板含 `CONFIG + STORIES + norm() 归一化 + 先查列表建映射 + 逐条 upsert + console.table`,补好两个数组即可粘贴运行。
- **title 归一化 `norm()`**:upsert 比对前抹平全角/半角括号与空白,避免 `(…)` vs `(…)` 被判成两条而重复建。
- **两个验证捷径**:① 创建 `result:success` 本身即证明 `needNotReview` 生效(评审开着没带评审人会 fail),不必再查评审人;② 批量同字段时抽一条详情页看「模块到二级 / 当前状态=激活·已计划」即可代表全批。
- 新增避坑 **错 9**(内网禅道 navigate 被组织策略拦,不绕过、不反复试,直接切模式 B)、**错 10**(title 不归一化导致重复)。
- 检查清单 D 增加「执行模式选择 / norm 归一化 / 模式 B 回传结果」;输出物清单补「拆分草稿 .md + 控制台粘贴脚本 .js」。

### 来源
- 通用「配置核查 V1.18.1」MVP1 真实同步:`dev.jugeng.com`(内网 http 禅道)上模式 A 的 navigate 被组织策略拦死,改模式 B 一次跑通 6/6(产品 75 / 模块 1004「运行管理·配置核查」/ 计划 138),落位字段全对。

---

## [1.9.0] - 2026-05-25

### 维护 / 基线对齐

- 作为后续工作的干净起点版本,无新增功能内容(功能口径与 1.8.0 一致)。
- 修复发布链路问题:此前 v1.7.0 / v1.8.0 的 tag 曾误指向旧 commit(1.6.1),且 `plugin.json` 版本号长期滞后于 `marketplace.json`。本版本起 `plugin.json` 与 `marketplace.json` 版本号严格保持一致,tag 与 main commit 内容对齐,避免 Cowork 拉到空壳 tag。

---

## [1.8.0] - 2026-05-22

### 新增(prd2prototype · 又一批实战标准 + 回写 PRD 共性)

- **时间格式统一** —— 所有时间戳一律 `YYYY-MM-DD HH:MM:SS`;纯日期 / 定时时段(每天 08:00)按本身语义保留,不强加秒;时间列宽放到 130-150px,不为省宽度砍精度。
- **同一概念跨页同名** —— 同一字段在列表 / 编辑 / 详情用同一个名(如列表"适用厂商/版本",编辑页拆"适用厂商"+"适用版本"呼应)。
- **详情入口都进只读模式** —— 与"编辑"复用同一页面靠 `mode=view` 区分,只读态隐藏保存 / 执行 / 删除、禁录入。
- **执行 / 触发 / 下发动作的交互** —— 对齐同类下发动作(如配置备份):先弹确认框(标题=动作名 /「确定要…吗?」/ 取消·确认),确认后就地改状态(结果列「执行中」)+ 按钮置灰,**不做列表内实时进度条**;执行入口收敛(一个动作一个入口,别在编辑器里重复放)。
- **内容展示口径** —— 配置 / 快照类("当前配置")展示视图内**实际配置片段**,缺失用"未见 XX 配置"体现,不用"未配置 XX"结论性文字充当;历史 / 累计明细**次数全量统计、明细只展示最近 N 天**(如 30 天)。
- **结构化积木的层级关系** —— 讲清"谁能挂在谁下 / 谁不能单独存在"(如数值比较只能挂在"必须包含 / 禁止包含"之下、隐式引用父匹配模式),写进规格表"关联关系"列。
- **回写 PRD 的共性(第 8 步)** —— 不是所有原型改动都回写 PRD(分"进 PRD 的产品规则·行为·范围" vs "原型为准的展示约定");回写前先在会话里列分级清单(必改 / 建议改 / 待拍板 / 不改)和用户对齐再动笔;改一处要 grep 全文排查连带矛盾;功能下沉的标准动作(原范围条目改下沉说明 + 版本规划新增完整条目 + 菜单·IA 加注 + 连带措辞同步);愿景 / 场景章节与 MVP 范围章节分层,范围变动时愿景不动。
- 检查清单同步新增 B / E / G 多项;新增「执行 / 触发 / 下发动作的交互」章节;conventions.md 必含项增加 时间格式 / 详情只读 / 执行动作 / 内容展示口径。

---

## [1.7.0] - 2026-05-21

### 新增(prd2prototype · 实战标准沉淀)

- **裁剪到本轮范围:隐藏而非删除** —— 非P0 用 `display:none` 隐藏并加注释,不真删;整页级非P0 隐藏入口;当心 P0 详情页因唯一入口被隐藏而孤立;裁剪后 grep 扫残留入口。
- **删除确认统一标准文案** —— 行删除 / 批量删除共用 `确定要删除所选数据信息吗?`,不做个性化影响说明。
- **记录类 / 查阅类列表默认不带前置多选框**,只有确有批量操作时才加多选列。
- **多对象批量操作 = 命令输入 + 设备列表 + 发送状态** —— 不画多终端实时广播窗口(实现成本高);命名贴着真实能力(批量 SSH),不起名"批量下发 / 配置下发"误导成新功能。
- **原型说明只讲本期范围** —— 不提 V2 / 计划执行 / 已完成等非本期内容,不写"(评审决议)"和"长度沿用某表"样板脚注;界面英文术语中文化(chip→标签)。
- **字段录入规格表** —— 有录入的页面在原型说明里放规格表(填写方式 / 必填 / 类型 / 长度 / 校验 / 数据源·单选多选 / 关联);数据源分清 枚举/字典/接口列表;有级联的拆两个字段(厂商+版本);长度不写"不限"。
- **复杂联动逻辑用「逻辑表 + 测试用例 + 静态示例」三件套讲清** —— 不硬做交互演示;命中条件别用伪代码(评审看不懂),用具体测试用例枚举组合 / 边界最直观。
- 检查清单同步新增 B / C / D / D2 / H 等多项;新增「操作按钮的二次确认」「裁剪到本轮范围」等章节。

---

## [1.6.1] - 2026-05-20

### 新增(requirements2prd · 需求规模前置判断)

- **新增「前置判断:需求规模定级」步骤**,在商业三问之前先区分需求类型:
  - **路径 A:大模块 / 新产品** → 走完整流程(商业本质三问 → 五看三定 → 七步法)
  - **路径 B:具体功能 / 页面级** → 跳过商业三问和五看三定,直接轻量细化需求(5W + 边界 + 约束 + 验收标准)
- 提供两个判断标准帮产品经理快速选路径:"会新增菜单/独立入口?" / "只影响某一页面内局部功能?"
- 商业本质三问标题更新为「路径 A 必答 · 路径 B 跳过」,避免对具体功能也走完整战略分析

### 来源
- 实战反馈:商业三问对大模块/新产品有价值,但对页面级小功能是过重的流程负担,导致产品经理阻力大、跳过 skill。前置分级后两类需求都有对应的合适路径。

---

## [1.5.0] - 2026-05-20

### 新增(prd2zentao · 第三个 skill)

- **新增 skill `prd2zentao`:PRD → 禅道研发需求(SR)的方法论**,补全"粗需求 → PRD → 原型 → 禅道需求"完整闭环。
  - **锚定 PRD 第四章「产品范围」拆需求**:不固化具体条目,按菜单/模块层级逐块映射,MVP1/MVP2 都套同一套。
  - **前置三件事(铁律)**:先确认目标产品 productID(不同产品不同目录,建错灾难性)、目标模块 moduleID、来源字段取值(需 PM 确认,不擅自拍板)。
  - **模块只到二级**:PRD 三级子菜单不映射成三级模块,需求统一挂二级模块下。
  - **API 批量 upsert**:复用「禅道Axure同步插件」的 zentao-client 逻辑,fetch + 会话 cookie + needNotReview,按 title 先查后建防重复。
  - **字段字典 + API 速查 + 可执行最小创建片段**全部内置。
  - 配套《前期操作手册》:装 Claude for Chrome 扩展、登录禅道、确认产品等前置步骤。
- **踩坑沉淀**:HTTP 200 ≠ 业务成功(需解析 result)、漏 needNotReview 会被"评审人不能为空"挡、JS 回传原始 HTML 会被隐私过滤 BLOCK(只回传判定字段)。

### 来源
- 配置核查 MVP1 的真实链路打通:8 条需求一次性建到测试产品的「配置核查」二级模块下,全程 API。

---

## [1.4.0] - 2026-05-19

### 新增(prd2prototype · 主会话 + 子 agent 协作模式)

- **新增「主会话 + 子 agent 协作模式」强制规范**,解决长会话 context 爆炸 + 跨页一致性两个核心痛点:
  - **主会话**作为单一会话连续推进,沉淀共性规则与跨页一致性
  - **子 agent**承接单页 / 批量页 / 审计等可隔离的执行性任务,context 不污染主会话
  - 子 agent 可并行,5 个页面一次性 spawn 5 个 agent
- **三层产物**:`conventions.md`(子 agent 的"宪法") + 页面原型 HTML(子 agent 产出) + 跨页一致性清单(主会话维护)
- **conventions.md 最小集**:颜色 / 命名规则 / 二次确认规则 / 不允许目的地不明的链接 / 统一中文术语 / common.css 类名约定 / 设计原则
- **标准操作流(SOP)**:规则对齐 → 任务分发 → 汇总 + 共性问题沉淀 → 你提反馈 → 批量整改,循环到定稿
- **划清不该走子 agent 的边界**:迭代微调 / 不确定的需求 / 小改 / 历史决策回顾 等仍由主会话做
- **主会话节省 context 的纪律**:不重读、不污染、大批量走 Bash/Python、切换主题前 /compact

### 来源
- 一次真实 B 端项目的 token 消耗事后分析:单会话累积 cache_read 达 577M tokens,主因是主会话一个人扛了所有页面级工作。把页面工作下沉到子 agent + 主会话只做编排和沉淀,实测 context 节省 30~40%

---

## [1.3.0] - 2026-05-19

### 新增(requirements2prd · PRD 文档结构标准 第六节)

- **新增「体量控制」约束**,防止 AI 写 PRD 长篇大论:
  - **PRD 主体目标:仔细评审 15 分钟读完(3,000 ~ 3,300 汉字)**
  - 评审会上所有人都要看完,超 15 分钟评审会就退化成"产品讲、其它人听"
  - 真正起作用的是"可验收的行为清单 + 数字事实",不是"为什么这样设计"的解说
- **提供具体的控压手法**:
  - 主体写完跑字数统计 `grep -o '[一-鿿]' file.md | wc -l`,超过 3,300 就逐章审视
  - 长描述合并成对比表(三视图各 200 字 → 对比表 1 行 80 字)
  - 删除"为什么这样设计"类解释段
  - 删除章节首段重复标题的导读句
- 附录不受 15 分钟约束(按需查阅,不是必读)

### 来源
- 一次真实 B 端项目的 PRD 评审节奏验证:5,000 字主体 → 仔细评审 26 分钟没人看完;压缩到 3,300 字 → 15~16 分钟可被多角色读完且决策质量更高

---

## [1.2.0] - 2026-05-19

### 新增(requirements2prd)
- **新增「PRD 文档结构标准」章节**(强制规范),沉淀真实项目走过几轮原型评审后回炉得到的 PRD 骨架:
  - **主体一级目录** 限定 5~6 章(背景目标 / 用户角色 / 核心痛点 / 产品范围 / 信息架构 / 版本规划),覆盖所有产品 PRD 通用骨架
  - **产品范围按菜单维度展开** —— xxx 页面有哪些功能项,而不是按"功能领域"罗列
  - **模块特有的设计机制** 下沉到附录 B(PDCA / 状态机 / DSL / 监管对照 等不进主体)
  - 明确指出 PRD 主体**不该有的内容**:UI 设计 / 实现细节 / 英文技术黑话(`running-configuration` / `VendorVersion` / `chip` / `modal` 等统一换中文)
- **新增"修订历史按阶段维护"约定**:v0.1 初稿 / v0.2 原型期累积修订 / v0.3 评审后定稿;原型期所有修改合并到同一 v0.2,不细分 v0.2/v0.3/v0.4
- **新增"产品决策 vs 功能描述"指引**:功能项写"可被勾选验收的具体行为",决策解释紧跟功能描述,不单开"设计原因"小节

### 来源
- 一次真实 B 端项目在原型走完几轮评审后,回炉重整 PRD 时积累的规范

---

## [1.1.2] - 2026-05-15

### 新增
- **`.claude-plugin/marketplace.json`** —— 让本仓库同时成为 Claude plugin marketplace
- 现在团队成员可以通过 **Cowork → Plugins → Personal → + → Add marketplace** 直接填 git URL `https://github.com/yideng-xl/jg-product-design-skills` 安装,**无需手动下载 zip + 上传**
- 仓库现在是"既是 marketplace 又是 plugin"的紧凑形态:marketplace.json 里 `source: "./"` 把当前仓库本身作为唯一的 plugin

### 影响
- 团队成员升级 plugin 不需要再下载 zip,直接在 Cowork 里 sync marketplace 即可拉到最新版
- 维护者发布流程不变(改 → 升版本 → push git → 团队 sync)

---

## [1.1.1] - 2026-05-15

### 修复(plugin 格式合规)
- `plugin.json` 移到 `.claude-plugin/` 目录下(此前在根目录导致 Cowork 上传时 "Plugin validation failed")
- 精简 `plugin.json` 字段,只保留 Claude 官方 plugin manifest 标准字段:name / version / description / author / homepage / repository / license / keywords
- 移除非标准的 `skills` / `displayName` / `bugs` / `changelog` 字段(skills 由 `skills/<name>/SKILL.md` 自动发现,无需在 manifest 里声明)
- License 从 "Internal" 改为 "MIT"(plugin 是公开仓库,跟 GitHub 仓库 license 对齐)

---

## [1.1.0] - 2026-05-15

### `requirements2prd` 新增章节
- **工作纪律(挨个过确认点 + 苏格拉底式引导)** —— 强调主动引导对面深入思考,逐项 AskUserQuestion 明确达成共识,不许默默推进
- **0 号问题:商业本质三问(降本/增效/合规)** —— 必答,作为需求是否该做的底线判断
- **战略视角:五看三定** —— 看用户/现状/法规/竞品/自己 + 定目标/边界/打法
- **历史债务/模块优化 子框架** —— 适配重构场景:现状盘点 + 痛点矩阵 + 改造路径选择(渐进/平行/整体) + 兼容性策略 + 显式废弃声明
- 检查清单升级为"逐项确认"形式,加 A-I 共 9 类

### `prd2prototype` 新增章节
- **第 8 步:回写 PRD + 锁定本轮迭代范围** —— 工作流的关键收尾
  - 8.1 把原型里的"新决策"回填进 PRD
  - 8.2 定义本轮迭代范围(本轮包含 / 本轮不包含 / 显式列出)
  - 8.3 找设计 / 技术 / 运维 / QA 确认(产品经理是迭代 owner 主动驱动)
- 检查清单加 G 类(评审后的回写)
- 输出物清单加回写后的 PRD 终稿 + 迭代范围定义 + 各方确认记录

### 通用化合规
- 全面去除特定行业领域属性词,确保 plugin 内容跨行业可复用

### 升级原因
团队对 plugin 实战使用提出 7 个改进点,本版本一次性吸收(版本号 MINOR 升级,无 breaking changes)。

---

## [1.0.0] - 2026-05-15

### 新增
- 初始版本,包含两个 skill:
  - **`requirements2prd`(粗需求到PRD)** —— 七步法 + 检查清单 + 5 类常犯错
  - **`prd2prototype`(PRD到原型)** —— 七步法 + 6 类检查清单 + 原型规范实战版 + 8 类常犯错
- plugin.json 元信息
- README 安装/触发/分发指南

### 来源
- 基于一个真实 B 端项目的完整复盘
- 累计沉淀 8 类犯错改进点 + 6 类对的做法

---

## 后续版本预期

每次在新模块上跑完一轮:

- **PATCH(1.0.x)**:补充踩坑案例、修改文案、修正笔误。skill 内容不变结构
- **MINOR(1.x.0)**:新增检查清单条目、新增"常犯的错"章节、引入新的设计规范
- **MAJOR(x.0.0)**:七步法步骤调整、引入新的 skill、删除已废弃的方法

升级版本号 → 更新本 CHANGELOG → push git → 团队 Claude 检测到新版本会提示用户更新。
